export interface NavType {
    label: string,
    path: string,
    icon: string,
    direct: 'left' | 'center' | 'right'
}

export interface KindType {
    label: string,
    value: string,
    icon: string,
}